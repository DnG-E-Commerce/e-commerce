const button = document.querySelectorAll('button.check')
const form = document.getElementById('form-checkout-cart')

form.addEventListener('submit', (e) => {
    e.preventDefault()
    button.forEach(data => {
        data.
    })
})
